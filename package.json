{
  "name": "nomad-budgeter",
  "version": "2.0.0",
  "description": "Digital Nomad Tax & Cost of Living Calculator — Powered by Eleventy",
  "scripts": {
    "pulse": "node scripts/pulse.mjs",
    "trends": "node scripts/generate-best-of.mjs",
    "dev": "npm run pulse && npm run trends && node ./node_modules/@11ty/eleventy/cmd.cjs --serve --port=3000",
    "build": "npm run pulse && npm run trends && node ./node_modules/@11ty/eleventy/cmd.cjs",
    "build:no-pulse": "node ./node_modules/@11ty/eleventy/cmd.cjs",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "dependencies": {
    "stripe": "^15.0.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.0",
    "@clerk/clerk-sdk-node": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
