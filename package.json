{
  "name": "nomad-budgeter",
  "version": "2.0.0",
  "description": "Digital Nomad Tax & Cost of Living Calculator — Powered by Eleventy",
  "scripts": {
    "cmo": "node --env-file=.env scripts/cmo-content-generator.mjs",
    "pulse": "node scripts/pulse.mjs",
    "trends": "node scripts/generate-best-of.mjs",
    "dev": "npm run pulse && node scripts/validate-build.mjs && npm run trends && node ./node_modules/@11ty/eleventy/cmd.cjs --serve --port=3000",
    "build": "npm run pulse && node scripts/validate-build.mjs && npm run trends && node ./node_modules/@11ty/eleventy/cmd.cjs",
    "build:no-pulse": "node ./node_modules/@11ty/eleventy/cmd.cjs",
    "minify:css": "postcss src/css/index.css -o _site/css/index.css",
    "minify:js": "terser _site/js/main.js -o _site/js/main.js --compress --mangle",
    "postbuild": "npm run minify:css && npm run minify:js",
    "clean": "rm -rf _site",
    "cmo:dry-run": "node --env-file=.env scripts/cmo-content-generator.mjs --dry-run"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "cssnano": "^8.0.1",
    "postcss": "^8.5.15",
    "postcss-cli": "^11.0.1",
    "terser": "^5.47.1"
  },
  "dependencies": {
    "@clerk/clerk-sdk-node": "^5.0.0",
    "axios": "^1.6.0",
    "stripe": "^15.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
